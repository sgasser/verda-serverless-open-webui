# Secrets (generate with: openssl rand -hex 32)
LITELLM_MASTER_KEY=sk-change-me
LITELLM_SALT_KEY=sk-change-me
SEARXNG_SECRET=change-me

# Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me
LITELLM_DB_PASSWORD=change-me
OPENWEBUI_DB_PASSWORD=change-me

# Domains
OPENWEBUI_DOMAIN=chat.your-domain.com
LITELLM_DOMAIN=litellm.your-domain.com

# Verda Serverless Endpoints
# Get these from Verda Console after deploying your containers
# Format: https://containers.verda.com/<deployment-name>
VERDA_LLM_ENDPOINT=https://containers.verda.com/your-vllm-deployment
VERDA_LLM_API_KEY=your-verda-inference-api-key

# Embeddings and Reranking are configured in Open WebUI Admin UI:
# Settings -> Documents -> Embedding Model -> External
# Settings -> Documents -> Reranking Model -> External
#
# Embedding Endpoint: https://containers.verda.com/<tei-embed-deployment>/v1
# Reranking Endpoint: https://containers.verda.com/<tei-rerank-deployment>

# Optional overrides (uncomment to change defaults)
# ENABLE_COMMUNITY_SHARING=true
# ENABLE_EVALUATION_ARENA_MODELS=true
# ENABLE_MESSAGE_RATING=true
